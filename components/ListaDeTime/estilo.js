import { StyleSheet } from 'react-native';

const estilo = StyleSheet.create({

  containerListaTimes: {
    flex: 1,
    padding: 10,
    backgroundColor: '#fff',
  },

  listaTimes: {
    padding: 10,
    marginBottom: 60,
  },

});

export default estilo;
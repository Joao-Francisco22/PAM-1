import { StyleSheet } from 'react-native';

const estilo = StyleSheet.create({

  caixaTitulo: {
    backgroundColor: '#000',
    alignItems: 'center',
    padding: 10,
  },

  textoTitulo: {
    fontSize: 28,
    color: '#61dafb',
  },

});

export default estilo;